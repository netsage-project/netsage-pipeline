#!/usr/bin/perl

use strict;
use warnings;

use GRNOC::NetSage::Anonymizer::FlowImporter;

use Getopt::Long;
use Data::Dumper;

### constants ###

use constant DEFAULT_CONFIG_FILE => '/etc/grnoc/netsage/anonymizer/config.xml';
use constant DEFAULT_LOGGING_FILE => '/etc/grnoc/netsage/anonymizer/logging.conf';

### command line options ###

my $config = DEFAULT_CONFIG_FILE;
my $logging = DEFAULT_LOGGING_FILE;
my $nofork;
my $jsonfile;
my $help;

GetOptions( 'config=s' => \$config,
            'logging=s' => \$logging,
            'nofork' => \$nofork,
            'jsonfile=s' => \$jsonfile,
            'help|h|?' => \$help );

# did they ask for help?
usage() if $help;

# start/daemonize writer
my $flow_importer = GRNOC::NetSage::Anonymizer::FlowImporter->new( config_file => $config,
                                                          logging_file => $logging,
                                                          daemonize => !$nofork,
                                                          jsonfile => $jsonfile );

$flow_importer->run();

### helpers ###

sub usage {

    print "Usage: $0 [--config <file path>] [--logging <file path>] [--jsonfile <file path>]\n";

    exit( 1 );
}
