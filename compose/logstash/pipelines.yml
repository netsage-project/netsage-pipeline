- pipeline.id: elastiflow
  path.config: "/usr/share/logstash/pipeline/*.conf"
  # set one of these... needs more updating to use v8
  #pipeline.ecs_compatibility: v8
  pipeline.ecs_compatibility: disabled

