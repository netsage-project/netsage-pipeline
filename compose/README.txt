
To update the containers, run:

cd ..
docker compose -f docker-compose.build.yml build

or depending on docker version

docker-compose -f docker-compose.build.yml build


and then push the containers to hub.docker.com
