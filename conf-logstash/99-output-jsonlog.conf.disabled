# output json blob of everything being sent to rabbit queue / elastic
output {
    file {
        path => "/data/output_data/test.json"
        codec => json_lines 
        #codec => json
        write_behavior => overwrite
    }
}
